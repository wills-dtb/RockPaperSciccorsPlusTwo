package model

case class HumanPlayer(
                      id: String,
                      rounds: Int,
                      name: String,
                      move: String
                      )
